---
name: add-component
description: Add a UI component, shared or feature-local. Use when creating a new component, or when deciding whether a component belongs in components/ui.
---

Read `docs/ai/add-component.md` and follow it exactly. Copy the structure of
`src/components/ui/Button.tsx` (the reference component) rather than inventing a props shape. Check
text contrast before introducing any colour. The user's request: $ARGUMENTS
